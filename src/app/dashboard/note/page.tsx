import NotesContainer from "@/components/note/index/NotesContainer";
import { Box } from "@mui/material";

export default function NotesPage() {
  
  return (
    <Box>
      <NotesContainer/>
    </Box>
  );
}